version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: voyager-backend-nginx
    restart: unless-stopped
    network_mode: host  # ⭐ Mismo que el backend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro